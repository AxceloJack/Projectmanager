{
  "buildCommand": "npx prisma generate --schema=apps/backend/prisma/schema.prisma && npm run build -w frontend",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
